# name: Build

# on:
#   push:
#     branches:
#       - main
#   pull_request:
#     types: [opened, synchronize, reopened]

# jobs:
#   sonarqube:
#     name: SonarQube
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Required for better Sonar analysis

#       - name: Set up Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 22.16.0  # Or match your local version

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Angular project
#         run: npm run build

#       - name: SonarQube Scan
#         uses: SonarSource/sonarqube-scan-action@v5
#         env:
#           SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}



name: Build

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]



jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0  
      
      - uses: actions/setup-node@v3
        with:
          node-version: 22.16.0  

      - name: Install dependencies
        run: npm ci

      - name: Build Angular project
        run: npm run build

      - uses: sonarsource/sonarqube-scan-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}


      - name: Check SonarQube Quality Gate
        uses: SonarSource/sonarqube-quality-gate-action@master
        id: qualitygate
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      - name: Fail if Quality Gate fails
        if: steps.qualitygate.outputs.quality_gate_status != 'PASSED'
        run: |
          echo "❌ Merge blocked: Quality Gate failed in SonarQube!"
          exit 1